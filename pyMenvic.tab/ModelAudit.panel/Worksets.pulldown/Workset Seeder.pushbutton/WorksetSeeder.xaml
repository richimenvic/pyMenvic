<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="pyMENVIC | Workset Seeder"
        Width="720"
        Height="640"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        Background="#1E252B">
    <Window.Resources>
        <SolidColorBrush x:Key="WindowBrush" Color="#1E252B" />
        <SolidColorBrush x:Key="HeaderBrush" Color="#26303A" />
        <SolidColorBrush x:Key="ContentBrush" Color="#F4F6F8" />
        <SolidColorBrush x:Key="PrimaryDarkTextBrush" Color="#1E252B" />
        <SolidColorBrush x:Key="PrimaryLightTextBrush" Color="#FFFFFF" />
        <SolidColorBrush x:Key="HeaderCardLabelBrush" Color="#8FA7BA" />
        <SolidColorBrush x:Key="FooterTextBrush" Color="#A9BAC8" />

        <Style x:Key="ToolButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Background" Value="#2F4B5E" />
            <Setter Property="BorderBrush" Value="#4C6B82" />
            <Setter Property="Height" Value="30" />
            <Setter Property="MinWidth" Value="96" />
            <Setter Property="Padding" Value="12,4" />
            <Setter Property="Margin" Value="0,0,6,0" />
        </Style>
        <Style x:Key="ApplyButtonStyle" TargetType="Button" BasedOn="{StaticResource ToolButtonStyle}">
            <Setter Property="Background" Value="#2D9D55" />
            <Setter Property="BorderBrush" Value="#49B571" />
        </Style>
        <Style x:Key="CloseButtonStyle" TargetType="Button" BasedOn="{StaticResource ToolButtonStyle}">
            <Setter Property="Background" Value="#2F4B5E" />
            <Setter Property="BorderBrush" Value="#4C6B82" />
        </Style>
        <Style x:Key="SectionLabelStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#1E252B" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Margin" Value="0,0,6,0" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#F7F9FB" />
            <Setter Property="Foreground" Value="#1E252B" />
            <Setter Property="BorderBrush" Value="#9FB3C1" />
            <Setter Property="Height" Value="30" />
            <Setter Property="Margin" Value="0,4,0,12" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>
        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="#FFFFFF" />
            <Setter Property="Foreground" Value="#1E252B" />
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Margin" Value="0,4,0,4" />
            <Setter Property="Foreground" Value="#1E252B" />
        </Style>
        <Style TargetType="ListView">
            <Setter Property="Background" Value="#F7F9FB" />
            <Setter Property="Foreground" Value="#1E252B" />
            <Setter Property="BorderBrush" Value="#C8D1D8" />
        </Style>
        <Style TargetType="ListViewItem">
            <Setter Property="Foreground" Value="#1E252B" />
            <Setter Property="Background" Value="#FFFFFF" />
            <Setter Property="Height" Value="28" />
            <Setter Property="Padding" Value="0" />
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#0A70B8" />
                    <Setter Property="Foreground" Value="White" />
                    <Setter Property="FontWeight" Value="SemiBold" />
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="GridViewColumnHeader">
            <Setter Property="Background" Value="#E9EEF3" />
            <Setter Property="Foreground" Value="#1E252B" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Padding" Value="6,4" />
        </Style>
    </Window.Resources>

    <Grid Background="{StaticResource WindowBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource HeaderBrush}" Padding="10" BorderThickness="0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="40" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Image x:Name="HeaderLogoImage" Width="28" Height="28" Stretch="Uniform" VerticalAlignment="Center" />
                <StackPanel Grid.Column="1" Margin="10,0,0,0" VerticalAlignment="Center">
                    <TextBlock Text="WORKSET SEEDER" FontSize="18" FontWeight="Bold" Foreground="{StaticResource PrimaryLightTextBrush}" />
                    <TextBlock Text="Create standard user worksets from office profiles." FontSize="11" Foreground="{StaticResource HeaderCardLabelBrush}" Margin="0,2,0,0" />
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="1" Background="{StaticResource ContentBrush}" Padding="18" BorderThickness="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Background="#FFFFFF" CornerRadius="6" Padding="14" Margin="0,0,0,12" BorderBrush="#C8D1D8" BorderThickness="1">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Margin="0,0,14,0">
                            <TextBlock Text="Profile" Style="{StaticResource SectionLabelStyle}" />
                            <ComboBox x:Name="ProfileCombo" />
                            <CheckBox x:Name="IncludeCoordinationCheck" Content="Include coordination worksets" />
                        </StackPanel>

                        <StackPanel Grid.Column="1" Margin="0,0,14,0">
                            <TextBlock Text="Discipline" Style="{StaticResource SectionLabelStyle}" />
                            <ComboBox x:Name="DisciplineCombo" />
                            <CheckBox x:Name="IncludeLinksCheck" Content="Include link worksets" />
                        </StackPanel>

                        <StackPanel Grid.Column="2" VerticalAlignment="Bottom">
                            <Button x:Name="RefreshButton" Content="Refresh" Style="{StaticResource ToolButtonStyle}" />
                        </StackPanel>
                    </Grid>
                </Border>

                <Border Grid.Row="1" Background="#FFFFFF" CornerRadius="6" Padding="14" BorderBrush="#C8D1D8" BorderThickness="1">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <Grid Grid.Row="0" Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Preview" FontSize="15" FontWeight="SemiBold" Foreground="{StaticResource PrimaryDarkTextBrush}" VerticalAlignment="Center" />
                            <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="10,0,0,0">
                                <TextBlock Text="Total " Foreground="#6B7C8D" />
                                <TextBlock x:Name="TotalCountText" Text="0" FontWeight="SemiBold" Foreground="{StaticResource PrimaryDarkTextBrush}" />
                            </StackPanel>
                            <StackPanel Grid.Column="2" Orientation="Horizontal" Margin="14,0,0,0">
                                <TextBlock Text="Missing " Foreground="#6B7C8D" />
                                <TextBlock x:Name="MissingCountText" Text="0" FontWeight="SemiBold" Foreground="{StaticResource PrimaryDarkTextBrush}" />
                            </StackPanel>
                            <StackPanel Grid.Column="3" Orientation="Horizontal" Margin="14,0,0,0">
                                <TextBlock Text="Existing " Foreground="#6B7C8D" />
                                <TextBlock x:Name="ExistingCountText" Text="0" FontWeight="SemiBold" Foreground="{StaticResource PrimaryDarkTextBrush}" />
                            </StackPanel>
                        </Grid>

                        <ListView x:Name="PreviewList" Grid.Row="1" BorderThickness="1" ScrollViewer.HorizontalScrollBarVisibility="Auto">
                            <ListView.View>
                                <GridView>
                                    <GridViewColumn Header="" Width="38">
                                        <GridViewColumn.CellTemplate>
                                            <DataTemplate>
                                                <CheckBox IsChecked="{Binding Selected}"
                                                          IsEnabled="{Binding IsSelectable}"
                                                          Width="18"
                                                          Height="18"
                                                          Margin="0"
                                                          HorizontalAlignment="Center"
                                                          VerticalAlignment="Center" />
                                            </DataTemplate>
                                        </GridViewColumn.CellTemplate>
                                    </GridViewColumn>
                                    <GridViewColumn Header="Workset" Width="420">
                                        <GridViewColumn.CellTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding Name}">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Foreground" Value="#1E252B" />
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType={x:Type ListViewItem}}, Path=IsSelected}" Value="True">
                                                                    <Setter Property="Foreground" Value="White" />
                                                                    <Setter Property="FontWeight" Value="SemiBold" />
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                            </DataTemplate>
                                        </GridViewColumn.CellTemplate>
                                    </GridViewColumn>
                                    <GridViewColumn Header="Status" Width="120">
                                        <GridViewColumn.CellTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding Status}">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Setter Property="Foreground" Value="#1E252B" />
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding Status}" Value="MISSING">
                                                                    <Setter Property="Foreground" Value="#B7791F" />
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding Status}" Value="EXISTS">
                                                                    <Setter Property="Foreground" Value="#2D9D55" />
                                                                </DataTrigger>
                                                                <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType={x:Type ListViewItem}}, Path=IsSelected}" Value="True">
                                                                    <Setter Property="Foreground" Value="White" />
                                                                    <Setter Property="FontWeight" Value="SemiBold" />
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                            </DataTemplate>
                                        </GridViewColumn.CellTemplate>
                                    </GridViewColumn>
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </Grid>
                </Border>
            </Grid>
        </Border>

        <Border Grid.Row="2" Background="{StaticResource WindowBrush}" Padding="18,10,18,12" BorderThickness="0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="StatusText" Text="Ready." VerticalAlignment="Center" Foreground="{StaticResource FooterTextBrush}" FontSize="12" />
                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="CreateButton" Content="Create Missing" Style="{StaticResource ApplyButtonStyle}" />
                    <Button x:Name="CloseButton" Content="Close" Style="{StaticResource CloseButtonStyle}" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
