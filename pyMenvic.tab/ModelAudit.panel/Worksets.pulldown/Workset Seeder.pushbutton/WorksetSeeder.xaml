<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="pyMENVIC | Workset Seeder"
        Width="720"
        Height="640"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        Background="#F4F6F8">
    <Window.Resources>
        <SolidColorBrush x:Key="AccentBrush" Color="#2F6B5F" />
        <SolidColorBrush x:Key="TextBrush" Color="#1F2933" />
        <SolidColorBrush x:Key="MutedBrush" Color="#6B7280" />
        <SolidColorBrush x:Key="CardBrush" Color="#FFFFFF" />
        <Style TargetType="Button">
            <Setter Property="MinWidth" Value="96" />
            <Setter Property="Height" Value="32" />
            <Setter Property="Margin" Value="4,0,0,0" />
            <Setter Property="Padding" Value="12,4" />
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Height" Value="30" />
            <Setter Property="Margin" Value="0,4,0,12" />
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Margin" Value="0,4,0,4" />
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
        </Style>
    </Window.Resources>

    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource CardBrush}" CornerRadius="10" Padding="16" BorderBrush="#D9DEE5" BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="56" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Image x:Name="HeaderLogoImage" Width="42" Height="42" Stretch="Uniform" VerticalAlignment="Center" />
                <StackPanel Grid.Column="1" Margin="12,0,0,0">
                    <TextBlock Text="WORKSET SEEDER" FontSize="20" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}" />
                    <TextBlock Text="Create standard user worksets from office profiles." FontSize="12" Foreground="{StaticResource MutedBrush}" Margin="0,4,0,0" />
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="1" Background="{StaticResource CardBrush}" CornerRadius="10" Padding="16" Margin="0,12,0,12" BorderBrush="#D9DEE5" BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,14,0">
                    <TextBlock Text="Profile" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}" />
                    <ComboBox x:Name="ProfileCombo" />
                    <CheckBox x:Name="IncludeCoordinationCheck" Content="Include coordination worksets" />
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="0,0,14,0">
                    <TextBlock Text="Discipline" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}" />
                    <ComboBox x:Name="DisciplineCombo" />
                    <CheckBox x:Name="IncludeLinksCheck" Content="Include link worksets" />
                </StackPanel>

                <StackPanel Grid.Column="2" VerticalAlignment="Bottom">
                    <Button x:Name="RefreshButton" Content="Refresh" />
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="2" Background="{StaticResource CardBrush}" CornerRadius="10" Padding="14" BorderBrush="#D9DEE5" BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid Grid.Row="0" Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Preview" FontSize="15" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}" VerticalAlignment="Center" />
                    <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="10,0,0,0">
                        <TextBlock Text="Total " Foreground="{StaticResource MutedBrush}" />
                        <TextBlock x:Name="TotalCountText" Text="0" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}" />
                    </StackPanel>
                    <StackPanel Grid.Column="2" Orientation="Horizontal" Margin="14,0,0,0">
                        <TextBlock Text="Missing " Foreground="{StaticResource MutedBrush}" />
                        <TextBlock x:Name="MissingCountText" Text="0" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}" />
                    </StackPanel>
                    <StackPanel Grid.Column="3" Orientation="Horizontal" Margin="14,0,0,0">
                        <TextBlock Text="Existing " Foreground="{StaticResource MutedBrush}" />
                        <TextBlock x:Name="ExistingCountText" Text="0" FontWeight="SemiBold" Foreground="{StaticResource TextBrush}" />
                    </StackPanel>
                </Grid>

                <ListView x:Name="PreviewList" Grid.Row="1" BorderBrush="#D9DEE5" BorderThickness="1" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="" Width="28">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <CheckBox IsChecked="{Binding Selected}" IsEnabled="{Binding IsSelectable}" HorizontalAlignment="Center" />
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="Workset" Width="430">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Name}" Foreground="#1F2933" />
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="Status" Width="120">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Status}">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="{StaticResource MutedBrush}" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Status}" Value="MISSING">
                                                            <Setter Property="Foreground" Value="#B7791F" />
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Status}" Value="EXISTS">
                                                            <Setter Property="Foreground" Value="#2F6B5F" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                        </GridView>
                    </ListView.View>
                </ListView>
            </Grid>
        </Border>

        <Grid Grid.Row="3" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="StatusText" Text="Ready." VerticalAlignment="Center" Foreground="{StaticResource MutedBrush}" />
            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="CreateButton" Content="Create Missing" Background="{StaticResource AccentBrush}" Foreground="White" />
                <Button x:Name="CloseButton" Content="Close" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
